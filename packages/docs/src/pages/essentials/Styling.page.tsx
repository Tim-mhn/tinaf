import { component } from 'tinaf/component';
import { DocPageLayout } from '../../layouts';

export const StylingPage = component(() => {
  return (
    <DocPageLayout title="Styling">
      <span> need to fill in </span>
    </DocPageLayout>
  );
});
