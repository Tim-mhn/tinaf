import { ReactiveValue } from './reactive/reactive';
import { Component } from './render-2';

/**
 *
 * const App = component(() => {
 *  const title = reactive('title')
 *
 *
 *   return div([
 *       div(title),
 *       div('content'),
 *       div('footer')
 *    ])
 * })
 *
 *
 */
